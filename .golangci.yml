run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gosec
    - misspell
    - prealloc
    - unconvert

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
