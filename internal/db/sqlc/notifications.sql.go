// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: notifications.sql

package sqlc

import (
	"context"
)

const createNotification = `-- name: CreateNotification :one
INSERT INTO notifications (id, org_id, user_id, event_name, subject_id, title, body, grouping_key)
VALUES (?, ?, ?, ?, ?, ?, ?, ?)
RETURNING id, org_id, user_id, event_name, subject_id, title, body, grouping_key, created_at, read_at
`

type CreateNotificationParams struct {
	ID          string
	OrgID       string
	UserID      string
	EventName   string
	SubjectID   string
	Title       string
	Body        string
	GroupingKey string
}

func (q *Queries) CreateNotification(ctx context.Context, arg CreateNotificationParams) (Notification, error) {
	row := q.db.QueryRowContext(ctx, createNotification,
		arg.ID,
		arg.OrgID,
		arg.UserID,
		arg.EventName,
		arg.SubjectID,
		arg.Title,
		arg.Body,
		arg.GroupingKey,
	)
	var i Notification
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.UserID,
		&i.EventName,
		&i.SubjectID,
		&i.Title,
		&i.Body,
		&i.GroupingKey,
		&i.CreatedAt,
		&i.ReadAt,
	)
	return i, err
}

const findGroupedNotification = `-- name: FindGroupedNotification :one
SELECT id, org_id, user_id, event_name, subject_id, title, body, grouping_key, created_at, read_at
FROM notifications
WHERE grouping_key = ? AND read_at = ''
ORDER BY created_at DESC
LIMIT 1
`

func (q *Queries) FindGroupedNotification(ctx context.Context, groupingKey string) (Notification, error) {
	row := q.db.QueryRowContext(ctx, findGroupedNotification, groupingKey)
	var i Notification
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.UserID,
		&i.EventName,
		&i.SubjectID,
		&i.Title,
		&i.Body,
		&i.GroupingKey,
		&i.CreatedAt,
		&i.ReadAt,
	)
	return i, err
}

const listNotifications = `-- name: ListNotifications :many
SELECT id, org_id, user_id, event_name, subject_id, title, body, grouping_key, created_at, read_at
FROM notifications
WHERE user_id = ?
ORDER BY created_at DESC
LIMIT ? OFFSET ?
`

type ListNotificationsParams struct {
	UserID string
	Limit  int64
	Offset int64
}

func (q *Queries) ListNotifications(ctx context.Context, arg ListNotificationsParams) ([]Notification, error) {
	rows, err := q.db.QueryContext(ctx, listNotifications, arg.UserID, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Notification
	for rows.Next() {
		var i Notification
		if err := rows.Scan(
			&i.ID,
			&i.OrgID,
			&i.UserID,
			&i.EventName,
			&i.SubjectID,
			&i.Title,
			&i.Body,
			&i.GroupingKey,
			&i.CreatedAt,
			&i.ReadAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listUnreadNotifications = `-- name: ListUnreadNotifications :many
SELECT id, org_id, user_id, event_name, subject_id, title, body, grouping_key, created_at, read_at
FROM notifications
WHERE user_id = ? AND read_at = ''
ORDER BY created_at DESC
LIMIT ?
`

type ListUnreadNotificationsParams struct {
	UserID string
	Limit  int64
}

func (q *Queries) ListUnreadNotifications(ctx context.Context, arg ListUnreadNotificationsParams) ([]Notification, error) {
	rows, err := q.db.QueryContext(ctx, listUnreadNotifications, arg.UserID, arg.Limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Notification
	for rows.Next() {
		var i Notification
		if err := rows.Scan(
			&i.ID,
			&i.OrgID,
			&i.UserID,
			&i.EventName,
			&i.SubjectID,
			&i.Title,
			&i.Body,
			&i.GroupingKey,
			&i.CreatedAt,
			&i.ReadAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const markAllNotificationsRead = `-- name: MarkAllNotificationsRead :exec
UPDATE notifications SET read_at = ?
WHERE user_id = ? AND read_at = ''
`

type MarkAllNotificationsReadParams struct {
	ReadAt string
	UserID string
}

func (q *Queries) MarkAllNotificationsRead(ctx context.Context, arg MarkAllNotificationsReadParams) error {
	_, err := q.db.ExecContext(ctx, markAllNotificationsRead, arg.ReadAt, arg.UserID)
	return err
}

const markNotificationRead = `-- name: MarkNotificationRead :exec
UPDATE notifications SET read_at = ?
WHERE id = ? AND user_id = ?
`

type MarkNotificationReadParams struct {
	ReadAt string
	ID     string
	UserID string
}

func (q *Queries) MarkNotificationRead(ctx context.Context, arg MarkNotificationReadParams) error {
	_, err := q.db.ExecContext(ctx, markNotificationRead, arg.ReadAt, arg.ID, arg.UserID)
	return err
}

const unreadNotificationCount = `-- name: UnreadNotificationCount :one
SELECT COUNT(*) FROM notifications
WHERE user_id = ? AND read_at = ''
`

func (q *Queries) UnreadNotificationCount(ctx context.Context, userID string) (int64, error) {
	row := q.db.QueryRowContext(ctx, unreadNotificationCount, userID)
	var count int64
	err := row.Scan(&count)
	return count, err
}
