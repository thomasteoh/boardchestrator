// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: sprints.sql

package sqlc

import (
	"context"
	"database/sql"
)

const closeSprint = `-- name: CloseSprint :one
UPDATE sprints SET state = 'closed'
WHERE id = ? AND project_id = ?
RETURNING id, org_id, project_id, name, starts_on, ends_on, state, created_at
`

type CloseSprintParams struct {
	ID        string
	ProjectID string
}

func (q *Queries) CloseSprint(ctx context.Context, arg CloseSprintParams) (Sprint, error) {
	row := q.db.QueryRowContext(ctx, closeSprint, arg.ID, arg.ProjectID)
	var i Sprint
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.ProjectID,
		&i.Name,
		&i.StartsOn,
		&i.EndsOn,
		&i.State,
		&i.CreatedAt,
	)
	return i, err
}

const createSprint = `-- name: CreateSprint :one
INSERT INTO sprints (id, org_id, project_id, name, starts_on, ends_on, state)
VALUES (?, ?, ?, ?, ?, ?, ?)
RETURNING id, org_id, project_id, name, starts_on, ends_on, state, created_at
`

type CreateSprintParams struct {
	ID        string
	OrgID     string
	ProjectID string
	Name      string
	StartsOn  string
	EndsOn    string
	State     string
}

func (q *Queries) CreateSprint(ctx context.Context, arg CreateSprintParams) (Sprint, error) {
	row := q.db.QueryRowContext(ctx, createSprint,
		arg.ID,
		arg.OrgID,
		arg.ProjectID,
		arg.Name,
		arg.StartsOn,
		arg.EndsOn,
		arg.State,
	)
	var i Sprint
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.ProjectID,
		&i.Name,
		&i.StartsOn,
		&i.EndsOn,
		&i.State,
		&i.CreatedAt,
	)
	return i, err
}

const findActiveSprint = `-- name: FindActiveSprint :one
SELECT id, org_id, project_id, name, starts_on, ends_on, state, created_at
FROM sprints
WHERE project_id = ? AND state = 'active'
ORDER BY starts_on DESC
LIMIT 1
`

func (q *Queries) FindActiveSprint(ctx context.Context, projectID string) (Sprint, error) {
	row := q.db.QueryRowContext(ctx, findActiveSprint, projectID)
	var i Sprint
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.ProjectID,
		&i.Name,
		&i.StartsOn,
		&i.EndsOn,
		&i.State,
		&i.CreatedAt,
	)
	return i, err
}

const findSprint = `-- name: FindSprint :one
SELECT id, org_id, project_id, name, starts_on, ends_on, state, created_at
FROM sprints
WHERE id = ? AND project_id = ?
`

type FindSprintParams struct {
	ID        string
	ProjectID string
}

func (q *Queries) FindSprint(ctx context.Context, arg FindSprintParams) (Sprint, error) {
	row := q.db.QueryRowContext(ctx, findSprint, arg.ID, arg.ProjectID)
	var i Sprint
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.ProjectID,
		&i.Name,
		&i.StartsOn,
		&i.EndsOn,
		&i.State,
		&i.CreatedAt,
	)
	return i, err
}

const findSprintByID = `-- name: FindSprintByID :one
SELECT id, org_id, project_id, name, starts_on, ends_on, state, created_at
FROM sprints
WHERE id = ?
`

func (q *Queries) FindSprintByID(ctx context.Context, id string) (Sprint, error) {
	row := q.db.QueryRowContext(ctx, findSprintByID, id)
	var i Sprint
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.ProjectID,
		&i.Name,
		&i.StartsOn,
		&i.EndsOn,
		&i.State,
		&i.CreatedAt,
	)
	return i, err
}

const listActiveSprints = `-- name: ListActiveSprints :many
SELECT s.id, s.org_id, s.project_id, s.name, s.starts_on, s.ends_on, s.state
FROM sprints s
WHERE s.state = 'active'
ORDER BY s.starts_on ASC
`

type ListActiveSprintsRow struct {
	ID        string
	OrgID     string
	ProjectID string
	Name      string
	StartsOn  string
	EndsOn    string
	State     string
}

// Active sprints across all projects/orgs for the daily snapshot job.
func (q *Queries) ListActiveSprints(ctx context.Context) ([]ListActiveSprintsRow, error) {
	rows, err := q.db.QueryContext(ctx, listActiveSprints)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListActiveSprintsRow
	for rows.Next() {
		var i ListActiveSprintsRow
		if err := rows.Scan(
			&i.ID,
			&i.OrgID,
			&i.ProjectID,
			&i.Name,
			&i.StartsOn,
			&i.EndsOn,
			&i.State,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listSprintSnapshots = `-- name: ListSprintSnapshots :many
SELECT id, sprint_id, project_id, org_id, taken_on, total_points, done_points, open_count, done_count, created_at
FROM sprint_snapshots
WHERE sprint_id = ?
ORDER BY taken_on ASC
`

func (q *Queries) ListSprintSnapshots(ctx context.Context, sprintID string) ([]SprintSnapshot, error) {
	rows, err := q.db.QueryContext(ctx, listSprintSnapshots, sprintID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []SprintSnapshot
	for rows.Next() {
		var i SprintSnapshot
		if err := rows.Scan(
			&i.ID,
			&i.SprintID,
			&i.ProjectID,
			&i.OrgID,
			&i.TakenOn,
			&i.TotalPoints,
			&i.DonePoints,
			&i.OpenCount,
			&i.DoneCount,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listSprints = `-- name: ListSprints :many
SELECT id, org_id, project_id, name, starts_on, ends_on, state, created_at
FROM sprints
WHERE project_id = ?
ORDER BY starts_on DESC
`

func (q *Queries) ListSprints(ctx context.Context, projectID string) ([]Sprint, error) {
	rows, err := q.db.QueryContext(ctx, listSprints, projectID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Sprint
	for rows.Next() {
		var i Sprint
		if err := rows.Scan(
			&i.ID,
			&i.OrgID,
			&i.ProjectID,
			&i.Name,
			&i.StartsOn,
			&i.EndsOn,
			&i.State,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const sprintTaskTotals = `-- name: SprintTaskTotals :one
SELECT
  COUNT(*) AS total_count,
  COALESCE(SUM(points), 0) AS total_points,
  COALESCE(SUM(CASE WHEN status = 'done' THEN points ELSE 0 END), 0) AS done_points,
  COALESCE(SUM(CASE WHEN status = 'done' THEN 1 ELSE 0 END), 0) AS done_count
FROM tasks
WHERE sprint_id = ? AND project_id = ?
`

type SprintTaskTotalsParams struct {
	SprintID  sql.NullString
	ProjectID string
}

type SprintTaskTotalsRow struct {
	TotalCount  int64
	TotalPoints interface{}
	DonePoints  interface{}
	DoneCount   interface{}
}

func (q *Queries) SprintTaskTotals(ctx context.Context, arg SprintTaskTotalsParams) (SprintTaskTotalsRow, error) {
	row := q.db.QueryRowContext(ctx, sprintTaskTotals, arg.SprintID, arg.ProjectID)
	var i SprintTaskTotalsRow
	err := row.Scan(
		&i.TotalCount,
		&i.TotalPoints,
		&i.DonePoints,
		&i.DoneCount,
	)
	return i, err
}

const updateSprint = `-- name: UpdateSprint :one
UPDATE sprints SET name = ?, starts_on = ?, ends_on = ?, state = ?
WHERE id = ? AND project_id = ?
RETURNING id, org_id, project_id, name, starts_on, ends_on, state, created_at
`

type UpdateSprintParams struct {
	Name      string
	StartsOn  string
	EndsOn    string
	State     string
	ID        string
	ProjectID string
}

func (q *Queries) UpdateSprint(ctx context.Context, arg UpdateSprintParams) (Sprint, error) {
	row := q.db.QueryRowContext(ctx, updateSprint,
		arg.Name,
		arg.StartsOn,
		arg.EndsOn,
		arg.State,
		arg.ID,
		arg.ProjectID,
	)
	var i Sprint
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.ProjectID,
		&i.Name,
		&i.StartsOn,
		&i.EndsOn,
		&i.State,
		&i.CreatedAt,
	)
	return i, err
}

const upsertSprintSnapshot = `-- name: UpsertSprintSnapshot :one
INSERT INTO sprint_snapshots (id, sprint_id, project_id, org_id, taken_on, total_points, done_points, open_count, done_count)
VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)
ON CONFLICT (sprint_id, taken_on) DO UPDATE SET
  total_points = excluded.total_points,
  done_points = excluded.done_points,
  open_count = excluded.open_count,
  done_count = excluded.done_count
RETURNING id, sprint_id, project_id, org_id, taken_on, total_points, done_points, open_count, done_count, created_at
`

type UpsertSprintSnapshotParams struct {
	ID          string
	SprintID    string
	ProjectID   string
	OrgID       string
	TakenOn     string
	TotalPoints int64
	DonePoints  int64
	OpenCount   int64
	DoneCount   int64
}

// WU-504: sprint snapshots (burndown/burnup).
func (q *Queries) UpsertSprintSnapshot(ctx context.Context, arg UpsertSprintSnapshotParams) (SprintSnapshot, error) {
	row := q.db.QueryRowContext(ctx, upsertSprintSnapshot,
		arg.ID,
		arg.SprintID,
		arg.ProjectID,
		arg.OrgID,
		arg.TakenOn,
		arg.TotalPoints,
		arg.DonePoints,
		arg.OpenCount,
		arg.DoneCount,
	)
	var i SprintSnapshot
	err := row.Scan(
		&i.ID,
		&i.SprintID,
		&i.ProjectID,
		&i.OrgID,
		&i.TakenOn,
		&i.TotalPoints,
		&i.DonePoints,
		&i.OpenCount,
		&i.DoneCount,
		&i.CreatedAt,
	)
	return i, err
}
