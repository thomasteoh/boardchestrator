// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: approvals.sql

package sqlc

import (
	"context"
	"database/sql"
)

const createApproval = `-- name: CreateApproval :one
INSERT INTO approvals (id, org_id, run_id, action_name, input_json, status)
VALUES (?, ?, ?, ?, ?, ?)
RETURNING id, org_id, run_id, action_name, input_json, status, requested_at, decided_by, decided_at
`

type CreateApprovalParams struct {
	ID         string
	OrgID      string
	RunID      string
	ActionName string
	InputJson  string
	Status     string
}

func (q *Queries) CreateApproval(ctx context.Context, arg CreateApprovalParams) (Approval, error) {
	row := q.db.QueryRowContext(ctx, createApproval,
		arg.ID,
		arg.OrgID,
		arg.RunID,
		arg.ActionName,
		arg.InputJson,
		arg.Status,
	)
	var i Approval
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.RunID,
		&i.ActionName,
		&i.InputJson,
		&i.Status,
		&i.RequestedAt,
		&i.DecidedBy,
		&i.DecidedAt,
	)
	return i, err
}

const decideApproval = `-- name: DecideApproval :one
UPDATE approvals
SET status = ?, decided_by = ?, decided_at = ?
WHERE id = ? AND org_id = ?
RETURNING id, org_id, run_id, action_name, input_json, status, requested_at, decided_by, decided_at
`

type DecideApprovalParams struct {
	Status    string
	DecidedBy sql.NullString
	DecidedAt sql.NullString
	ID        string
	OrgID     string
}

func (q *Queries) DecideApproval(ctx context.Context, arg DecideApprovalParams) (Approval, error) {
	row := q.db.QueryRowContext(ctx, decideApproval,
		arg.Status,
		arg.DecidedBy,
		arg.DecidedAt,
		arg.ID,
		arg.OrgID,
	)
	var i Approval
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.RunID,
		&i.ActionName,
		&i.InputJson,
		&i.Status,
		&i.RequestedAt,
		&i.DecidedBy,
		&i.DecidedAt,
	)
	return i, err
}

const findApprovalByID = `-- name: FindApprovalByID :one
SELECT id, org_id, run_id, action_name, input_json, status, requested_at, decided_by, decided_at
FROM approvals
WHERE id = ? AND org_id = ?
`

type FindApprovalByIDParams struct {
	ID    string
	OrgID string
}

func (q *Queries) FindApprovalByID(ctx context.Context, arg FindApprovalByIDParams) (Approval, error) {
	row := q.db.QueryRowContext(ctx, findApprovalByID, arg.ID, arg.OrgID)
	var i Approval
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.RunID,
		&i.ActionName,
		&i.InputJson,
		&i.Status,
		&i.RequestedAt,
		&i.DecidedBy,
		&i.DecidedAt,
	)
	return i, err
}

const findApprovalForRun = `-- name: FindApprovalForRun :one
SELECT id, org_id, run_id, action_name, input_json, status, requested_at, decided_by, decided_at
FROM approvals
WHERE run_id = ? AND org_id = ? AND action_name = ? AND input_json = ?
ORDER BY requested_at DESC
LIMIT 1
`

type FindApprovalForRunParams struct {
	RunID      string
	OrgID      string
	ActionName string
	InputJson  string
}

func (q *Queries) FindApprovalForRun(ctx context.Context, arg FindApprovalForRunParams) (Approval, error) {
	row := q.db.QueryRowContext(ctx, findApprovalForRun,
		arg.RunID,
		arg.OrgID,
		arg.ActionName,
		arg.InputJson,
	)
	var i Approval
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.RunID,
		&i.ActionName,
		&i.InputJson,
		&i.Status,
		&i.RequestedAt,
		&i.DecidedBy,
		&i.DecidedAt,
	)
	return i, err
}

const findPendingApprovalForRun = `-- name: FindPendingApprovalForRun :one
SELECT id, org_id, run_id, action_name, input_json, status, requested_at, decided_by, decided_at
FROM approvals
WHERE run_id = ? AND org_id = ? AND action_name = ? AND input_json = ? AND status = 'pending'
`

type FindPendingApprovalForRunParams struct {
	RunID      string
	OrgID      string
	ActionName string
	InputJson  string
}

func (q *Queries) FindPendingApprovalForRun(ctx context.Context, arg FindPendingApprovalForRunParams) (Approval, error) {
	row := q.db.QueryRowContext(ctx, findPendingApprovalForRun,
		arg.RunID,
		arg.OrgID,
		arg.ActionName,
		arg.InputJson,
	)
	var i Approval
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.RunID,
		&i.ActionName,
		&i.InputJson,
		&i.Status,
		&i.RequestedAt,
		&i.DecidedBy,
		&i.DecidedAt,
	)
	return i, err
}

const listApprovalsByRun = `-- name: ListApprovalsByRun :many
SELECT id, org_id, run_id, action_name, input_json, status, requested_at, decided_by, decided_at
FROM approvals
WHERE run_id = ? AND org_id = ?
ORDER BY requested_at ASC
`

type ListApprovalsByRunParams struct {
	RunID string
	OrgID string
}

func (q *Queries) ListApprovalsByRun(ctx context.Context, arg ListApprovalsByRunParams) ([]Approval, error) {
	rows, err := q.db.QueryContext(ctx, listApprovalsByRun, arg.RunID, arg.OrgID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Approval
	for rows.Next() {
		var i Approval
		if err := rows.Scan(
			&i.ID,
			&i.OrgID,
			&i.RunID,
			&i.ActionName,
			&i.InputJson,
			&i.Status,
			&i.RequestedAt,
			&i.DecidedBy,
			&i.DecidedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listPendingApprovalsByOrg = `-- name: ListPendingApprovalsByOrg :many
SELECT id, org_id, run_id, action_name, input_json, status, requested_at, decided_by, decided_at
FROM approvals
WHERE org_id = ? AND status = 'pending'
ORDER BY requested_at ASC
LIMIT ?
`

type ListPendingApprovalsByOrgParams struct {
	OrgID string
	Limit int64
}

func (q *Queries) ListPendingApprovalsByOrg(ctx context.Context, arg ListPendingApprovalsByOrgParams) ([]Approval, error) {
	rows, err := q.db.QueryContext(ctx, listPendingApprovalsByOrg, arg.OrgID, arg.Limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Approval
	for rows.Next() {
		var i Approval
		if err := rows.Scan(
			&i.ID,
			&i.OrgID,
			&i.RunID,
			&i.ActionName,
			&i.InputJson,
			&i.Status,
			&i.RequestedAt,
			&i.DecidedBy,
			&i.DecidedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
