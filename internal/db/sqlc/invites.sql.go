// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: invites.sql

package sqlc

import (
	"context"
	"database/sql"
)

const acceptInvite = `-- name: AcceptInvite :one
UPDATE invites SET accepted_at = ?
WHERE id = ? AND accepted_at IS NULL
RETURNING id, org_id, inviter_id, email, token_hash, role_id, resource_type, resource_id, expires_at, accepted_at, created_at
`

type AcceptInviteParams struct {
	AcceptedAt sql.NullString
	ID         string
}

func (q *Queries) AcceptInvite(ctx context.Context, arg AcceptInviteParams) (Invite, error) {
	row := q.db.QueryRowContext(ctx, acceptInvite, arg.AcceptedAt, arg.ID)
	var i Invite
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.InviterID,
		&i.Email,
		&i.TokenHash,
		&i.RoleID,
		&i.ResourceType,
		&i.ResourceID,
		&i.ExpiresAt,
		&i.AcceptedAt,
		&i.CreatedAt,
	)
	return i, err
}

const createInvite = `-- name: CreateInvite :one
INSERT INTO invites (id, org_id, inviter_id, email, token_hash, role_id, resource_type, resource_id, expires_at)
VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)
RETURNING id, org_id, inviter_id, email, token_hash, role_id, resource_type, resource_id, expires_at, accepted_at, created_at
`

type CreateInviteParams struct {
	ID           string
	OrgID        string
	InviterID    string
	Email        string
	TokenHash    string
	RoleID       sql.NullString
	ResourceType string
	ResourceID   string
	ExpiresAt    string
}

func (q *Queries) CreateInvite(ctx context.Context, arg CreateInviteParams) (Invite, error) {
	row := q.db.QueryRowContext(ctx, createInvite,
		arg.ID,
		arg.OrgID,
		arg.InviterID,
		arg.Email,
		arg.TokenHash,
		arg.RoleID,
		arg.ResourceType,
		arg.ResourceID,
		arg.ExpiresAt,
	)
	var i Invite
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.InviterID,
		&i.Email,
		&i.TokenHash,
		&i.RoleID,
		&i.ResourceType,
		&i.ResourceID,
		&i.ExpiresAt,
		&i.AcceptedAt,
		&i.CreatedAt,
	)
	return i, err
}

const deleteInvite = `-- name: DeleteInvite :exec
DELETE FROM invites
WHERE id = ? AND org_id = ?
`

type DeleteInviteParams struct {
	ID    string
	OrgID string
}

func (q *Queries) DeleteInvite(ctx context.Context, arg DeleteInviteParams) error {
	_, err := q.db.ExecContext(ctx, deleteInvite, arg.ID, arg.OrgID)
	return err
}

const findInviteByID = `-- name: FindInviteByID :one
SELECT id, org_id, inviter_id, email, token_hash, role_id, resource_type, resource_id, expires_at, accepted_at, created_at
FROM invites
WHERE id = ?
`

func (q *Queries) FindInviteByID(ctx context.Context, id string) (Invite, error) {
	row := q.db.QueryRowContext(ctx, findInviteByID, id)
	var i Invite
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.InviterID,
		&i.Email,
		&i.TokenHash,
		&i.RoleID,
		&i.ResourceType,
		&i.ResourceID,
		&i.ExpiresAt,
		&i.AcceptedAt,
		&i.CreatedAt,
	)
	return i, err
}

const findInviteByTokenHash = `-- name: FindInviteByTokenHash :one
SELECT id, org_id, inviter_id, email, token_hash, role_id, resource_type, resource_id, expires_at, accepted_at, created_at
FROM invites
WHERE token_hash = ?
`

func (q *Queries) FindInviteByTokenHash(ctx context.Context, tokenHash string) (Invite, error) {
	row := q.db.QueryRowContext(ctx, findInviteByTokenHash, tokenHash)
	var i Invite
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.InviterID,
		&i.Email,
		&i.TokenHash,
		&i.RoleID,
		&i.ResourceType,
		&i.ResourceID,
		&i.ExpiresAt,
		&i.AcceptedAt,
		&i.CreatedAt,
	)
	return i, err
}

const findPendingInvitesByOrg = `-- name: FindPendingInvitesByOrg :many
SELECT id, org_id, inviter_id, email, token_hash, role_id, resource_type, resource_id, expires_at, accepted_at, created_at
FROM invites
WHERE org_id = ? AND accepted_at IS NULL AND expires_at > ?
ORDER BY created_at
`

type FindPendingInvitesByOrgParams struct {
	OrgID     string
	ExpiresAt string
}

func (q *Queries) FindPendingInvitesByOrg(ctx context.Context, arg FindPendingInvitesByOrgParams) ([]Invite, error) {
	rows, err := q.db.QueryContext(ctx, findPendingInvitesByOrg, arg.OrgID, arg.ExpiresAt)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Invite
	for rows.Next() {
		var i Invite
		if err := rows.Scan(
			&i.ID,
			&i.OrgID,
			&i.InviterID,
			&i.Email,
			&i.TokenHash,
			&i.RoleID,
			&i.ResourceType,
			&i.ResourceID,
			&i.ExpiresAt,
			&i.AcceptedAt,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
